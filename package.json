{
  "name": "kotlin-spark",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "marp --engine ./engine.js index.md",
    "build-pdf": "marp --engine ./engine.js --allow-local-files --pdf index.md",
    "build-pptx": "marp --engine ./engine.js --allow-local-files --pptx index.md",
    "serve": "marp server --watch --engine ./engine.js index.md"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^0.17.0"
  },
  "dependencies": {
    "markdown-it-highlight-lines": "^1.0.2"
  }
}
